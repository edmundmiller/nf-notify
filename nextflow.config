// Find the version
def version = new File('plugins/nf-notify/src/resources/META-INF/MANIFEST.MF')
    .readLines()
    .find { it.startsWith('Plugin-Version:') }
    ?.tokenize(':')[1]
    ?.trim()

plugins {
    id "nf-notify@${version}"
}

notify {
    enabled = true
}
